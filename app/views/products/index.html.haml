= render "topbar"
= render "header"
= render "main"
= render "footer"
= link_to "products#new", class: "sellBtn" do
  %span.sellBtn__text 出品する
  = image_tag "/material/icon/icon_camera.png", class: "sellBtn__image"
